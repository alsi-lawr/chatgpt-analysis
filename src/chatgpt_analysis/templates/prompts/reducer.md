# Reducer contract

Reducers operate on validated cards, not raw model prose. Deterministically union additive labels and individually valid evidence anchors across primary and independent review. Preserve both scalar hypothesis ratings as a range. Keep one macro-weighted record per chat. Use the primary reviewer's relevance tier as the canonical scalar and retain secondary disagreement only as audit/sensitivity metadata. Treat every existing third-review output as audit-only, regardless of coverage.
