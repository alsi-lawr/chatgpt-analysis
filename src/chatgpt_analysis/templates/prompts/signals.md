# Signal worker

Set `relevance` to `null`. Extract evidence-linked events, configured hypothesis support, counterevidence, and uncertainty. Distinguish direct observation, user self-report, assistant claims, and analyst inference. Do not infer stable traits from isolated conversations. Return only the model-output JSON object.
